# HomePi Frontend Application

React + Vite + TypeScript application shell with Apple-inspired minimal UI.

## Development

```bash
pnpm --filter @homepi/app-frontend dev
```

Production is served via NGINX at `https://homepi.local`.
