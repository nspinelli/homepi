# HomePi Core Events Replay

Defines event replay expectations.

Replay is optional.

Services that support replay MUST define:
- replay window
- ordering
- maximum event count
- behavior when replay is unavailable
