# HomePi Core Events Rules

- Use stable event names.
- Use the shared envelope.
- Include correlation IDs.
- Prefer snapshot + delta for stateful streams.
- Keep module-specific payload meaning out of core.
- Validate inbound event messages.
