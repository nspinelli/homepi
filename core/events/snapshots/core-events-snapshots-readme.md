# HomePi Core Events Snapshots

Defines snapshot behavior.

A snapshot is an authoritative state payload emitted before delta events.

Snapshot examples:
- service state snapshot
- module state snapshot
- frontend initial sync snapshot
