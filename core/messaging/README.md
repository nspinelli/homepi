# core/messaging

Shared NDJSON socket messaging contracts for HomePi services.

- Schemas: [`schema/`](schema/)
- TypeScript: [`ts/`](ts/)

Commands are direct. Events are fanout. This library is not a runtime service.
