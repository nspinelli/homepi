{
  "name": "@homepi/core-service-registry",
  "version": "0.1.0",
  "type": "module",
  "description": "HomePi static service registry loader and types",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "cp ../registry.json ./registry.json && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@homepi/tooling-schema-validation": "workspace:*",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
