{
  "name": "@homepi/core-state",
  "version": "0.1.0",
  "type": "module",
  "description": "HomePi state snapshot and delta helpers",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@homepi/tooling-schema-validation": "workspace:*",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
