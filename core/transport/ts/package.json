{
  "name": "@homepi/core-transport",
  "version": "0.1.0",
  "type": "module",
  "description": "HomePi transport envelopes, NDJSON framing, and reconnect helpers",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@homepi/tooling-schema-validation": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
