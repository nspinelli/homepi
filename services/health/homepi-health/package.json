{
  "name": "@homepi/service-health",
  "version": "0.1.0",
  "type": "module",
  "description": "HomePi systemd and service health observer",
  "bin": {
    "homepi-health": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@homepi/core-logging": "workspace:*",
    "@homepi/core-messaging": "workspace:*",
    "@homepi/core-service-registry": "workspace:*",
    "@homepi/core-transport": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
