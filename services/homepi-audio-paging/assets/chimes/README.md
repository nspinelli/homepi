# Default Chime Asset

The production `default.wav` chime is generated by `scripts/install.sh` at install time
when `/var/lib/homepi/paging/chimes/default.wav` does not already exist.

This avoids committing binary WAV content to the repository while still ensuring a valid
bundled default chime is present on deployed systems.
