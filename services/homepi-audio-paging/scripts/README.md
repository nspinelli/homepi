# Install Scripts

`install.sh` builds and deploys `homepi-audio-paging` under `/opt/homepi/services/audio-paging`,
installs systemd unit `homepi-audio-paging.service`, and ensures paging voice/chime directories exist.
