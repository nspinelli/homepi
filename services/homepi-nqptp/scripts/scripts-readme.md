# homepi-nqptp scripts

| Script | Purpose |
|--------|---------|
| `install.sh` | Clone upstream nqptp, build to `/opt/homepi/services/nqptp`, install systemd unit, verify |
| `uninstall.sh` | Stop service, remove unit and install tree |

Run with root: `sudo bash install.sh`
