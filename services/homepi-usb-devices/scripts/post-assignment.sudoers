# Allow homepi service user to run post-assignment hook without a password.
Defaults:homepi !requiretty
Defaults env_keep += "HOMEPI_ALLOW_REBOOT"
Defaults env_keep += "HOMEPI_REBOOT_DELAY_SEC"
homepi ALL=(ALL) NOPASSWD: /opt/homepi/services/usb-devices/scripts/post-assignment-hook.sh
